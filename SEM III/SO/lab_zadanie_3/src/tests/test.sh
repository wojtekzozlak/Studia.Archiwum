grep "ODWOŁANIE DO STRONY" log | wc --lines
grep "INICJACJA ZAPISU" log | wc --lines
grep "KONIEC ZAPISU STRONY" log | wc --lines
grep "INICJACJA ODCZYTU STRONY" log | wc --lines
grep "KONIEC ODCZYTU STRONY" log | wc --lines
grep "KONIEC OPERACJI NA STRONIE" log | wc --lines
